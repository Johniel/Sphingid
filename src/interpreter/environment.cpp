#include "environment.hpp"

namespace sphingid {
  namespace environment {

    class Environment {
    public:
      Environment();
      virtual ~Environment();
    private:
      // std::map<std::string, >
    };

  }
}
